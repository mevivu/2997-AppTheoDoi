<div class="text-center">
    <span class="fw-bold text-primary fs-14">{{ $total_entries ?? 0 }}</span>
    <div class="text-muted fs-11" title="{{ __('Số lượt hoàn thành hợp lệ thắng 4/4 ván') }}">
        ({{ $valid_entries ?? 0 }} {{ __('hợp lệ') }})
    </div>
</div>
